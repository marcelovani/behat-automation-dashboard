<?php

/**
 * @file Used to implement hooks for Dashboard module.
 */

/**
 * Implements hook_handlebars_templates().
 */
function dashboard_handlebars_templates($context) {
  // Attach Handlebars templates to every page.
  return [
    'dashboard.failures' => 'dashboard',
  ];
}
